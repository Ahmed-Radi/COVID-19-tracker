import React from "react";
import ReactDOM from "react-dom";

import App from "./App";

ReactDOM.render(<App />, document.getElementById('root'));/**the Second parameter is the element you want to render on the App component -> this ID->' root ' I got from index.html in a public file*/